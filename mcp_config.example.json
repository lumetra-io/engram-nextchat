{
  "mcpServers": {
    "engram": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://mcp.lumetra.io/mcp/sse",
        "--header",
        "Authorization:Bearer eng_live_..."
      ]
    }
  }
}
